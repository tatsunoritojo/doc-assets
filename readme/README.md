# readme/

GitHub README 用の**焼き込み画像**（PNG）。

| ファイル | 内容 |
|----------|------|
| `on-white-*.png` | 白背景 + カラー/線画アイコン帯 |
| `concept.svg` / `layers.svg` / `usage.svg` | 図メインの説明 |

再生成: macOS の `qlmanage` + Pillow（`scripts/render_readme_strips.py` があればそれを使う）。
